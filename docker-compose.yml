version: '3'

services:
  node-app:
    build: .
    image: node-app
    environment:
      - MONGODB_URL=mongodb://mongodb:27017/woofie-chat
      - PORT=3000
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    ports:
      - 3000:3000
    depends_on:
      - mongodb
      - redis
    volumes:
      - ./runzone/node-app/server:/code
      # - ./runzone/node-app/server:/usr/src/node-app
    networks:
      - woofie-chat-network

  mongodb:
    image: mongo:4.2.1-bionic
    ports:
      - 27017:27017
    volumes:
      - ./runzone/storage/mongo/data:/data/db
    networks:
      - woofie-chat-network

  redis:
    container_name: woofie-redis
    image: redis
    ports:
      - 6379:6379
    volumes:
      - ./runzone/storage/redis/data:/data
    networks:
      - woofie-chat-network

volumes:
  dbdata:

networks:
  woofie-chat-network:
    driver: bridge
